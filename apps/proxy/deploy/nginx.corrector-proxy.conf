# Example Nginx config for the Corrector proxy.
# Replace YOUR_DOMAIN with your actual hostname.

server {
    listen 80;
    server_name YOUR_DOMAIN;

    client_max_body_size 64k;

    location /v1/ {
        proxy_pass http://127.0.0.1:8787;
        proxy_read_timeout 30s;
        proxy_connect_timeout 5s;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Request-Id $request_id;
    }

    location /health {
        allow all;
        proxy_pass http://127.0.0.1:8787;
        proxy_read_timeout 30s;
        proxy_connect_timeout 5s;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Request-Id $request_id;
    }
}

# HTTPS example. After running certbot, it will typically manage these blocks for you.
server {
    listen 443 ssl;
    server_name YOUR_DOMAIN;

    client_max_body_size 64k;

    # ssl_certificate /etc/letsencrypt/live/YOUR_DOMAIN/fullchain.pem;
    # ssl_certificate_key /etc/letsencrypt/live/YOUR_DOMAIN/privkey.pem;

    location /v1/ {
        proxy_pass http://127.0.0.1:8787;
        proxy_read_timeout 30s;
        proxy_connect_timeout 5s;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Request-Id $request_id;
    }

    location /health {
        allow all;
        proxy_pass http://127.0.0.1:8787;
        proxy_read_timeout 30s;
        proxy_connect_timeout 5s;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Request-Id $request_id;
    }
}

# HTTPS note: use certbot to provision certificates and update this configuration.
